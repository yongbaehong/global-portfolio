// ==========================================================================
// TRANSLATION DICTIONARY
// ==========================================================================
const translations = {
  en: {
    "nav-about": "About",
    "nav-skills": "Skills",
    "nav-experience": "Experience",
    "nav-projects": "Projects",
    "nav-contact": "Contact",

    "hero-badge-text": "Open for Global Opportunities",
    "hero-title-hello": "Hi, I'm",
    "hero-title-name": "Yongbae",
    "hero-description-text":
      "I build high-performance web applications with clean architecture and modern user experiences. Bridging the gap between robust backend systems and beautiful frontend designs.",
    "hero-cta-projects": "View Projects",
    "hero-cta-contact": "Get In Touch",

    "about-title": "About Me",
    "about-subtitle": "My journey, values, and why we should work together.",
    "about-exp-badge": "Stack &<br>Modern Web",
    "about-heading-text": "Engineering solutions with a global perspective",
    "about-p1":
      "I am a software engineer based in Pyeongtaek, South Korea, specializing in building modern web applications. With experience working in multicultural environments, I have developed a strong communication mindset and an adaptable approach to technology.",
    "about-p2":
      "I believe in writing clean, maintainable code, optimizing performance, and designing intuitive user interfaces. My goal is to build digital products that solve real-world problems and scale effectively under load.",
    "about-label-education": "Education",
    "about-value-education":
      "A.A. in Liberal Arts - Behavioral Sciences, Autodidactic Software Engineer",
    "about-label-languages": "Languages",
    "about-value-languages":
      "English (Native), Korean (Basic - Conversational and Technical Proficiency)",
    "about-label-interests": "Specialty",
    "about-value-interests": "SPA Architecture, RESTful APIs, MERN Stack",
    "about-label-work": "Work Preference",
    "about-value-work": "Remote, Hybrid",

    "skills-title": "Technical Skills",
    "skills-subtitle":
      "My toolbox of technologies and languages that I use to bring ideas to life.",
    "skills-cat-frontend": "Frontend",
    "skills-cat-backend": "Backend",
    "skills-cat-tools": "DevOps & Tools",
    "skills-familiar": "Other Tech I Work With:",

    "projects-title": "Featured Projects",
    "projects-subtitle":
      "A collection of systems and applications I've developed, from concept to deployment.",
    "project1-title": "Job Finder Web Application (MERN Stack)",
    "project1-desc-1":
      "Architected a full-stack, dynamic job board application featuring user authentication, job posting workflows, and a responsive search interface.",
    "project1-desc-2":
      "Built dynamic filtering components with React and Express to handle complex queries across thousands of mock job listings.",
    "project1-desc-3":
      "Optimized backend REST API endpoints to reduce data payload sizes and improve client-side rendering speed.",
    "project2-title": "Community Chat: Real-Time Chat App (MERN & Socket.io)",
    "project2-desc-1":
      "Developed a scalable, real-time messaging application supporting instant user interaction, room creation, and photo-sharing capabilities.",
    "project2-desc-2":
      "Implemented event-driven architecture using Socket.io to manage persistent, low-latency bidirectional communication.",
    "project2-desc-3":
      "Integrated image uploading and optimized state updates to ensure smooth rendering.",
    "project-details-btn": "View Details",

    "contact-title": "Get In Touch",
    "contact-subtitle":
      "Have a job opportunity, project idea, or just want to connect? Drop a message!",
    "contact-info-heading": "Contact Information",
    "contact-info-text":
      "I am actively seeking full-time opportunities as a Full Stack / Frontend / Backend Engineer. Let's build something remarkable.",
    "contact-label-email": "Email",
    "contact-label-phone": "Phone / KakaoTalk",
    "contact-value-phone": "+82 (0)10-9401-5678",
    "contact-label-location": "Location",
    "contact-value-location":
      "Pyeongtaek, South Korea (Available for Relocation)",
    "contact-form-label-name": "Name",
    "contact-form-label-email": "Email",
    "contact-form-label-subject": "Subject",
    "contact-form-label-message": "Message",
    "contact-form-submit": "Send Message",
    "footer-rights": "All rights reserved.",
  },
  ko: {
    "nav-about": "소개",
    "nav-skills": "기술",
    "nav-experience": "경력",
    "nav-projects": "프로젝트",
    "nav-contact": "연락처",

    "hero-badge-text": "글로벌 기업 지원 중 / 원격 근무 가능",
    "hero-title-hello": "안녕하세요, 저는",
    "hero-title-name": "용배입니다",
    "hero-description-text":
      "깔끔한 아키텍처와 현대적인 사용자 경험을 바탕으로 고성능 웹 애플리케이션을 개발합니다. 강력한 백엔드 시스템과 미려한 프론트엔드 디자인 간의 가교 역할을 지향합니다.",
    "hero-cta-projects": "프로젝트 보기",
    "hero-cta-contact": "연락하기",

    "about-title": "소개",
    "about-subtitle":
      "제 커리어 여정과 가치관, 그리고 저와 함께 일해야 하는 이유를 소개합니다.",
    "about-exp-badge": "스택 &<br>최신 웹",
    "about-heading-text": "글로벌 관점으로 엔지니어링 솔루션을 만듭니다",
    "about-p1":
      "대한민국 평택에 거주하는 소프트웨어 엔지니어로, 최신 웹 애플리케이션 구축에 주력하고 있습니다. 다양한 문화적 환경에서 일한 경험을 통해 원활한 의사소통 태도와 기술에 유연하게 적응하는 능력을 길렀습니다.",
    "about-p2":
      "깔끔하고 유지보수가 쉬운 코드 작성, 성능 최적화, 그리고 직관적인 UI 설계의 중요성을 깊이 이해하고 있습니다. 실제 사용자의 비즈니스 문제를 해결하고 대규모 트래픽에도 유연하게 대처하는 제품 개발을 목표로 삼고 있습니다.",
    "about-label-education": "학력",
    "about-value-education":
      "문과대학 행동과학 전문학사, 독학으로 공부한 소프트웨어 엔지니어",
    "about-label-languages": "사용 언어",
    "about-value-languages":
      "영어 (모국어), 한국어 (기초 - 일상 회화 및 기술 커뮤니케이션 가능)",
    "about-label-interests": "주요 전문 역량",
    "about-value-interests": "SPA 아키텍처, RESTful API, MERN 스택",
    "about-label-work": "근무 형태",
    "about-value-work": "원격, 하이브리드",

    "skills-title": "기술 스택",
    "skills-subtitle":
      "아이디어를 실질적인 프로덕트로 구현하기 위해 사용하는 핵심 기술 도구 모음입니다.",
    "skills-cat-frontend": "프론트엔드",
    "skills-cat-backend": "백엔드",
    "skills-cat-tools": "데브옵스 및 도구",
    "skills-familiar": "그 외 다룰 수 있는 기술:",

    "projects-title": "대표 프로젝트",
    "projects-subtitle":
      "아이디어 기획부터 제품 빌드 및 배포까지 총괄적으로 참여해 온 핵심 프로젝트 리스트입니다.",

    "project1-title": "채용 정보 웹 애플리케이션 (MERN 스택)",
    "project1-desc-1":
      "사용자 인증, 채용공고 등록 흐름, 반응형 검색 인터페이스를 갖춘 풀스택 채용 정보 플랫폼을 설계했습니다.",
    "project1-desc-2":
      "React와 Express로 동적 필터링 컴포넌트를 구현해 수천 건의 목업 채용공고를 효율적으로 탐색할 수 있게 만들었습니다.",
    "project1-desc-3":
      "백엔드 REST API 엔드포인트를 최적화해 데이터 페이로드를 줄이고 클라이언트 렌더링 속도를 개선했습니다.",
    "project2-title": "커뮤니티 채팅: 실시간 채팅 앱 (MERN & Socket.io)",
    "project2-desc-1":
      "실시간 메시지 송수신, 채팅방 생성, 사진 공유 기능을 지원하는 확장 가능한 실시간 채팅 애플리케이션을 개발했습니다.",
    "project2-desc-2":
      "Socket.io 기반 이벤트 드리븐 아키텍처를 구현해 지속적이고 저지연의 양방향 통신을 관리했습니다.",
    "project2-desc-3":
      "이미지 업로드를 통합하고 상태 업데이트를 최적화해 원활한 렌더링 경험을 제공했습니다.",
    "project-details-btn": "상세 보기",

    "contact-title": "연락하기",
    "contact-subtitle":
      "비즈니스 기회 제안, 프로젝트 관련 논의, 혹은 기술 네트워킹은 언제든지 환영합니다!",
    "contact-info-heading": "연락처 정보",
    "contact-info-text":
      "현재 풀스택, 프론트엔드 또는 백엔드 엔지니어 정규직 기회를 적극적으로 찾고 있습니다. 함께 놀라운 결과물을 만들어 보세요.",
    "contact-label-email": "이메일",
    "contact-label-phone": "전화 / 카카오톡",
    "contact-value-phone": "+82 (0)10-9401-5678",
    "contact-label-location": "활동 지역",
    "contact-value-location": "대한민국 평택 (이주 가능)",
    "contact-form-label-name": "이름",
    "contact-form-label-email": "이메일 주소",
    "contact-form-label-subject": "문의 제목",
    "contact-form-label-message": "문의 내용",
    "contact-form-submit": "메시지 보내기",
    "footer-rights": "모든 권리 보유.",
  },
};

// Subtitle typing lists for bilingual support
const typedStrings = {
  en: [
    "Full Stack Developer",
    "UX Enthusiast",
    "Global Collaborator",
    "Problem Solver",
  ],
  ko: ["풀스택 개발자", "UX 엔지니어", "글로벌 협력형 인재", "문제 해결사"],
};

// ==========================================================================
// STATE VARIABLES
// ==========================================================================
let currentLang = "en";
let typingIndex = 0;
let charIndex = 0;
let isDeleting = false;
let typedTimeout = null;

// ==========================================================================
// THEME MANAGEMENT (Light / Dark)
// ==========================================================================
function initTheme() {
  const savedTheme = localStorage.getItem("theme") || "dark";
  const body = document.body;

  if (savedTheme === "light") {
    body.classList.remove("dark-theme");
    body.classList.add("light-theme");
  } else {
    body.classList.remove("light-theme");
    body.classList.add("dark-theme");
  }
}

function toggleTheme() {
  const body = document.body;
  if (body.classList.contains("dark-theme")) {
    body.classList.remove("dark-theme");
    body.classList.add("light-theme");
    localStorage.setItem("theme", "light");
  } else {
    body.classList.remove("light-theme");
    body.classList.add("dark-theme");
    localStorage.setItem("theme", "dark");
  }
}

// ==========================================================================
// LANGUAGE SWITCHER
// ==========================================================================
function switchLanguage(lang) {
  if (lang !== "en" && lang !== "ko") return;

  currentLang = lang;
  localStorage.setItem("lang", lang);

  // Toggle active classes on language buttons
  const enBtn = document.getElementById("lang-en-btn");
  const koBtn = document.getElementById("lang-ko-btn");

  if (lang === "en") {
    enBtn.classList.add("active");
    koBtn.classList.remove("active");
    document.body.classList.remove("lang-ko");
    document.body.classList.add("lang-en");
  } else {
    koBtn.classList.add("active");
    enBtn.classList.remove("active");
    document.body.classList.remove("lang-en");
    document.body.classList.add("lang-ko");
  }

  // Translate HTML elements
  const i18nElements = document.querySelectorAll("[data-i18n]");
  i18nElements.forEach((elem) => {
    const key = elem.getAttribute("data-i18n");
    if (translations[lang] && translations[lang][key]) {
      // Check if element has child HTML or just text
      if (
        elem.innerHTML.includes("<span") ||
        elem.innerHTML.includes("<i") ||
        elem.innerHTML.includes("<br")
      ) {
        // Keep the icon or structure if necessary
        if (key === "about-exp-badge") {
          elem.innerHTML = translations[lang][key];
        } else if (key === "hero-cta-projects") {
          elem.innerHTML =
            translations[lang][key] +
            ' <i class="fas fa-arrow-right icon-right"></i>';
        } else if (key === "contact-form-submit") {
          elem.innerHTML =
            translations[lang][key] +
            ' <i class="fas fa-paper-plane icon-right"></i>';
        } else if (key === "hero-title-hello") {
          elem.textContent = translations[lang][key];
        } else if (key === "hero-title-name") {
          elem.textContent = translations[lang][key];
        } else {
          elem.textContent = translations[lang][key];
        }
      } else {
        // Text replacement
        if (elem.tagName === "INPUT" || elem.tagName === "TEXTAREA") {
          elem.placeholder = translations[lang][key];
        } else {
          elem.textContent = translations[lang][key];
        }
      }
    }
  });

  // Reset typing animation for the newly selected language
  typingIndex = 0;
  charIndex = 0;
  isDeleting = false;
  if (typedTimeout) clearTimeout(typedTimeout);
  typeText();
}

function initLanguage() {
  const savedLang = localStorage.getItem("lang") || "en";
  switchLanguage(savedLang);
}

// ==========================================================================
// TYPING EFFECT ANIMATION
// ==========================================================================
function typeText() {
  const typedTextSpan = document.getElementById("typed-text");
  if (!typedTextSpan) return;

  const strings = typedStrings[currentLang];
  const currentString = strings[typingIndex];

  if (isDeleting) {
    // Erase character
    typedTextSpan.textContent = currentString.substring(0, charIndex - 1);
    charIndex--;
  } else {
    // Type character
    typedTextSpan.textContent = currentString.substring(0, charIndex + 1);
    charIndex++;
  }

  let typingSpeed = isDeleting ? 40 : 80;

  if (!isDeleting && charIndex === currentString.length) {
    // Pause at the end of word
    typingSpeed = 2000;
    isDeleting = true;
  } else if (isDeleting && charIndex === 0) {
    isDeleting = false;
    // Move to the next string index
    typingIndex = (typingIndex + 1) % strings.length;
    typingSpeed = 500;
  }

  typedTimeout = setTimeout(typeText, typingSpeed);
}

// ==========================================================================
// SCROLL EVENTS (Header blur + Active Navigation highlight)
// ==========================================================================
function handleScroll() {
  const header = document.getElementById("header");
  if (!header) return;

  if (window.scrollY > 50) {
    header.classList.add("scrolled");
  } else {
    header.classList.remove("scrolled");
  }

  // Highlight Active Links in Navigation Menu
  const sections = document.querySelectorAll("section[id]");
  const scrollPosition = window.scrollY + 200; // offset

  sections.forEach((section) => {
    const top = section.offsetTop;
    const height = section.offsetHeight;
    const id = section.getAttribute("id");

    if (scrollPosition >= top && scrollPosition < top + height) {
      const activeLink = document.querySelector(`.nav-link[href="#${id}"]`);
      if (activeLink) {
        document
          .querySelectorAll(".nav-link")
          .forEach((link) => link.classList.remove("active"));
        activeLink.classList.add("active");
      }
    }
  });
}

// ==========================================================================
// INTERACTIVE SKILL BARS ON SCROLL
// ==========================================================================
function animateSkillBars() {
  const skillsSection = document.getElementById("skills");
  if (!skillsSection) return;

  const sectionPosition = skillsSection.getBoundingClientRect().top;
  const screenPosition = window.innerHeight / 1.2;

  if (sectionPosition < screenPosition) {
    const skillFills = document.querySelectorAll(".skill-bar-fill");
    skillFills.forEach((bar) => {
      const targetWidth =
        bar.parentElement.previousElementSibling.lastElementChild.textContent;
      bar.style.width = targetWidth;
    });
    // Remove listener once completed
    window.removeEventListener("scroll", animateSkillBars);
  }
}

// ==========================================================================
// MOBILE MENU TOGGLE
// ==========================================================================
function initMobileMenu() {
  const toggleBtn = document.getElementById("mobile-menu-toggle");
  const navMenu = document.getElementById("nav-menu");
  const navLinks = document.querySelectorAll(".nav-link");

  if (!toggleBtn || !navMenu) return;

  toggleBtn.addEventListener("click", () => {
    navMenu.classList.toggle("active");

    // Switch hamburger icon to Close icon
    const icon = toggleBtn.querySelector("i");
    if (navMenu.classList.contains("active")) {
      icon.className = "fas fa-times";
    } else {
      icon.className = "fas fa-bars";
    }
  });

  // Close mobile menu when clicking a link
  navLinks.forEach((link) => {
    link.addEventListener("click", () => {
      navMenu.classList.remove("active");
      const icon = toggleBtn.querySelector("i");
      if (icon) icon.className = "fas fa-bars";
    });
  });
}

// ==========================================================================
// PROJECTS FILTERING
// ==========================================================================
function initProjectFilters() {
  const filterBtns = document.querySelectorAll(".filter-btn");
  const projectCards = document.querySelectorAll(".project-card");

  filterBtns.forEach((btn) => {
    btn.addEventListener("click", () => {
      // Remove active classes
      filterBtns.forEach((b) => b.classList.remove("active"));
      btn.classList.add("active");

      const filterValue = btn.getAttribute("data-filter");

      projectCards.forEach((card) => {
        const cardCategory = card.getAttribute("data-category");

        if (filterValue === "all" || cardCategory === filterValue) {
          card.classList.remove("hidden");
        } else {
          card.classList.add("hidden");
        }
      });
    });
  });
}

// ==========================================================================
// CONTACT FORM SUBMISSION (MAILTO)
// ==========================================================================
function initContactForm() {
  const form = document.getElementById("contact-form");

  if (!form) return;

  form.addEventListener("submit", (e) => {
    e.preventDefault();

    const name = document.getElementById("form-name").value;
    const email = document.getElementById("form-email").value;
    const subject = document.getElementById("form-subject").value;
    const message = document.getElementById("form-message").value;

    // Validate all fields are filled
    if (!name || !email || !subject || !message) {
      alert(
        currentLang === "en"
          ? "Please fill out all fields."
          : "모든 입력창을 채워주세요.",
      );
      return;
    }

    // Build mailto link with form data
    const mailtoSubject = encodeURIComponent(`${subject}`);
    const mailtoBody = encodeURIComponent(
      `Name: ${name}\nEmail: ${email}\n\nMessage:\n${message}`,
    );
    const mailtoLink = `mailto:codingyongbae@gmail.com?subject=${mailtoSubject}&body=${mailtoBody}`;

    // Open email client
    window.location.href = mailtoLink;

    // Reset form after opening email
    setTimeout(() => {
      form.reset();
    }, 500);
  });
}

// ==========================================================================
// DOCUMENT INIT
// ==========================================================================
document.addEventListener("DOMContentLoaded", () => {
  initTheme();
  initLanguage();
  initMobileMenu();
  initProjectFilters();
  initContactForm();

  // Add scroll event listeners
  window.addEventListener("scroll", handleScroll);
  window.addEventListener("scroll", animateSkillBars);

  // Initial triggers for scrolling items
  handleScroll();
  animateSkillBars();

  // Theme toggle button click
  const themeBtn = document.getElementById("theme-toggle");
  if (themeBtn) {
    themeBtn.addEventListener("click", toggleTheme);
  }

  // Language buttons click
  const enBtn = document.getElementById("lang-en-btn");
  const koBtn = document.getElementById("lang-ko-btn");

  if (enBtn) enBtn.addEventListener("click", () => switchLanguage("en"));
  if (koBtn) koBtn.addEventListener("click", () => switchLanguage("ko"));
});
